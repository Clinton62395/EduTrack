import { Box, Text } from "@/components/ui/theme";
import { SafeAreaView } from "react-native-safe-area-context";

export default function TrainerAttendance() {
  return (
    <SafeAreaView>
      <Box padding="m">
        <Text> Trainer attendance </Text>
      </Box>
    </SafeAreaView>
  );
}
