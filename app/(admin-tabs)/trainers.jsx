import { Box, Text } from "@/components/ui/theme";
import { SafeAreaView } from "react-native-safe-area-context";
export default function AdminTrainers() {
  return (
    <SafeAreaView>
      <Box padding="m">
        <Text> Dashboard des formateurs (Admin) </Text>
      </Box>
    </SafeAreaView>
  );
}
