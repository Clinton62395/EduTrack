import { Box, Text } from "@/components/ui/theme";
import { SafeAreaView } from "react-native-safe-area-context";
export default function LearnerProfile() {
  return (
    <SafeAreaView>
      <Box padding="m">
        <Text> Profil de l'apprenant </Text>
      </Box>
    </SafeAreaView>
  );
}
